public class Main {
   public static void main(String[] args) {
      int temperature = 50;
      int target = 0;
      // In the while loop, we must get to the correct temperature and exit.
      while (true) {
         temperature -= 5;
         /* Add the missing statements */
         System.out.println("Temperature is " + temperature);
         if (temperature <= target) {
            /* Add the missing statements */
         }
      }
   }
}