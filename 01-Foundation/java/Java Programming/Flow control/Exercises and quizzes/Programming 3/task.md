# Boucles `for` et `while`

Soit le programme suivant :

```
for (int i = 1; i <= 4; i++) {
  for (int j = 1; j <= i; j++) {
    System.out.print("*");
  }
  System.out.println();
}
```

Vous devez écrire un programme qui produit le même résultat à l'aide de deux 
boucles `while`.

#