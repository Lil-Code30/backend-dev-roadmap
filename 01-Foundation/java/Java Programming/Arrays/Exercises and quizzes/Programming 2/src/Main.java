public class Main {
   public static void main(String[] args) {
      // Array declaration and initialization
      final int BASE = 5;
    Define the array type array = Initialize the array
      for(Complete the for loop parameters) {
         array[i] = Complete the assignation
         for(Complete the for loop parameters) {
            arrayComplete the array assignation = '*';
         }
      }
      // Display
      for(Complete the for-each loop) {
         for(Complete the for-each loop) {
            System.out.print(c);
         }
         System.out.println();
      }
   }
}