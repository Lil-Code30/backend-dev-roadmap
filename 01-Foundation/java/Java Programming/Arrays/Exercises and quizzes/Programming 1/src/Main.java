import java.util.Arrays;

public class Main {
   public static void main(String[] args) {
      int size = 10;
      Define array type array = Initialize array
      for(Complete the for loop parameters) {
         Write a statement to assign the array values
      }
      System.out.println(Arrays.toString(array));
   }
}