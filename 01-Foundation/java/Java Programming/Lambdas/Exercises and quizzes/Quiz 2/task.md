# Expression lambda et référence de méthode

Quel est l'équivalent de l'expression lambda suivante en référence de méthode ?
```
Arrays.sort(array, (a, b) -> {
   return f(a).compareTo(f(b));
});
```

#