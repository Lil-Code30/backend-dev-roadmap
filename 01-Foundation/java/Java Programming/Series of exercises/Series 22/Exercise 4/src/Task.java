import java.io.File;
import java.util.Arrays;

public class Task {
    public static void main(String[] args) {
        String path = "Series of exercises/Series 22/Exercise 4";
        if (args.length > 0) {
            path = args[0];
        }
        File f = new File(path);
        String[] images;
        /* Implement the filtering of all files in the current directory that ends with ".jpg" or ".gif" or ".png" */ implemented");
        }
        System.out.println(Arrays.toString(images));
    }}