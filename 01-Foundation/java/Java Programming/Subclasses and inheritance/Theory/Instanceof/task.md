# Instanceof

L'opérateur `instanceof` permet de tester l'appartenance d'un objet à une 
classe donnée à l'exécution. Cela permet d'éviter une erreur fatale à 
l'exécution. 

# Exemple
La classe `Main` illustre une utilisation possible de l'opérateur `instanceof`.